# test_large_space_repeated 函数汇编代码详解

## 注意事项

容器环境是 **ARM64 架构**，objdump 不支持 `-M intel` 选项（该选项仅用于 x86_64）。
以下是 ARM64 汇编代码的详细分析。

## 如何生成汇编代码

### 1. 进入容器环境

```bash
docker-compose exec benchmark /bin/bash
```

### 2. 编译程序

```bash
gcc -O2 -g -o /tmp/stack_growth_comparison src/stack_growth_comparison.c
```

编译选项说明：
- `-O2`：启用优化（验证编译器优化行为）
- `-g`：生成调试信息（保留函数符号）
- `-o /tmp/stack_growth_comparison`：输出到临时目录

### 3. 反汇编查看函数

```bash
objdump -d /tmp/stack_growth_comparison | awk '/^[0-9a-f]* <test_large_space_repeated>:/,/^$/ {print}' | head -200
```

命令说明：
- `objdump -d`：反汇编所有代码段
- `awk '/^...$/,/^$/ {print}'`：提取 test_large_space_repeated 函数（从函数头到空行）
- `head -200`：显示前 200 行（足够显示完整函数）

### 4. 完整命令（在宿主机执行）

```bash
docker-compose exec -T benchmark bash -c "gcc -O2 -g -o /tmp/stack_growth_comparison src/stack_growth_comparison.c && objdump -d /tmp/stack_growth_comparison | awk '/^[0-9a-f]* <test_large_space_repeated>:/,/^$/ {print}' | head -200"
```

## 完整汇编代码（带注释）

```asm
0000000000000e40 <test_large_space_repeated>:

;; ========================================
;; 第一部分：函数序言 - 保存寄存器和分配栈空间
;; ========================================
 e40:   stp   x29, x30, [sp, #-32]!    ; 保存帧指针(x29)和返回地址(x30), sp -= 32
 e44:   mov   x29, sp                  ; 设置帧指针 x29 = sp
 e48:   adrp  x0, 1f000 <__FRAME_END__+0x1d674>
 e4c:   ldr   x0, [x0, #4024]
 e50:   str   x19, [sp, #16]           ; 保存 x19 寄存器
 e54:   str   d15, [sp, #24]           ; 保存 d15 浮点寄存器

;; ⭐⭐⭐ 关键1：一次性分配整个 800KB 栈空间
 e58:   sub   sp, sp, #0x30            ; sp -= 48 字节（局部变量空间）
 e5c:   sub   sp, sp, #0xc8, lsl #12   ; ⭐ sp -= (0xc8 << 12) = 0xc8000 = 819,200 bytes = 800KB
                                        ; 这是唯一的栈空间分配！

 e60:   add   x1, sp, #0xc8, lsl #12   ; x1 = sp + 800KB（用于栈保护检查）
 e64:   ldr   x2, [x0]                 ; 加载栈保护金丝雀值
 e68:   str   x2, [x1, #40]            ; 存储金丝雀值
 e6c:   mov   x2, #0x0                 ; x2 = 0

;; ========================================
;; 第二部分：打印信息（可忽略）
;; ========================================
 e70:   adrp  x0, 1000 <_fini+0x38>
 e74:   add   x0, x0, #0x240
 e78:   bl    7d0 <puts@plt>           ; 打印标题
 e7c:   mov   w2, #0xc8                ; w2 = 200 (总页数)
 e80:   mov   w3, #0xa                 ; w3 = 10 (重复次数)
 e84:   mov   w1, #0x320               ; w1 = 800 (KB)
 e88:   adrp  x0, 1000 <_fini+0x38>
 e8c:   add   x0, x0, #0x270
 e90:   bl    7b0 <printf@plt>         ; 打印配置信息
 e94:   adrp  x0, 1000 <_fini+0x38>
 e98:   add   x0, x0, #0x2b8
 e9c:   bl    7d0 <puts@plt>
 ea0:   mov   w1, #0xc8
 ea4:   adrp  x0, 1000 <_fini+0x38>
 ea8:   add   x0, x0, #0x310
 eac:   bl    7b0 <printf@plt>

;; ========================================
;; 第三部分：开始计时
;; ========================================
 eb0:   add   x1, sp, #0x8             ; x1 = &start_time
 eb4:   mov   w0, #0x1                 ; w0 = CLOCK_MONOTONIC
 eb8:   bl    7a0 <clock_gettime@plt>  ; 获取开始时间

;; ========================================
;; 第四部分：外层循环准备
;; ========================================
 ebc:   add   x5, sp, #0xc8, lsl #12   ; ⭐ x5 = sp + 800KB（数组末尾边界）
 ec0:   adrp  x2, 20000 <__dso_handle>
 ec4:   add   x5, x5, #0x28            ; x5 = 数组末尾地址（sp + 800KB + 0x28）
 ec8:   add   x2, x2, #0x40            ; x2 = &g_counter（全局计数器地址）
 ecc:   mov   w6, #0xa                 ; ⭐ w6 = 10（外层循环计数器）

;; ========================================
;; 第五部分：外层循环入口（循环10次）
;; ========================================
;; ⭐⭐⭐ 关键2：外层循环入口，注意这里 sp 不再改变！
 ed0:   add   x0, sp, #0x28            ; ⭐ x0 = 数组基地址 = sp + 0x28
                                        ; ⭐ 由于 sp 不变，每次迭代 x0 都是相同的地址！
 ed4:   mov   w1, #0x42                ; w1 = 0x42（写入的初始值 = 66）
 ed8:   nop                             ; 对齐填充
 edc:   nop                             ; 对齐填充

;; ========================================
;; 第六部分：内层循环 - 遍历200页（每页4KB）
;; ========================================
 ee0:   strb  w1, [x0]                 ; ⭐ 写页首: *x0 = w1
 ee4:   strb  w1, [x0, #4095]          ; ⭐ 写页尾: *(x0 + 4095) = w1
 ee8:   add   w1, w1, #0x1             ; w1++ (递增写入值)

 eec:   ldrb  w3, [x0]                 ; ⭐ 读页首: w3 = *x0
 ef0:   add   x0, x0, #0x1, lsl #12    ; ⭐ x0 += 4096 (移动到下一页)
 ef4:   ldr   w4, [x2]                 ; 读取全局计数器
 ef8:   add   w3, w4, w3, uxtb         ; 累加
 efc:   str   w3, [x2]                 ; 写回全局计数器

 f00:   ldurb w3, [x0, #-1]            ; ⭐ 读页尾: w3 = *(x0 - 1) = *(prev_page + 4095)
 f04:   ldr   w4, [x2]                 ; 读取全局计数器
 f08:   add   w3, w4, w3, uxtb         ; 累加
 f0c:   str   w3, [x2]                 ; 写回全局计数器

 f10:   cmp   x5, x0                   ; ⭐ 比较：是否到达数组末尾？
 f14:   b.ne  ee0 <...+0xa0>           ; ⭐ 否，跳转到内层循环开始（0xee0）

;; ========================================
;; 第七部分：外层循环控制
;; ========================================
 f18:   subs  w6, w6, #0x1             ; ⭐ w6--（外层计数器递减）
 f1c:   b.ne  ed0 <...+0x90>           ; ⭐⭐⭐ 关键3：跳回外层循环入口（0xed0）
                                        ; ⭐ 注意：这里跳过了栈分配代码（0xe5c）！
                                        ; ⭐ sp 保持不变，重复使用相同的 800KB 栈内存

;; ========================================
;; 第八部分：结束计时
;; ========================================
 f20:   add   x1, sp, #0x18            ; x1 = &end_time
 f24:   mov   w0, #0x1                 ; w0 = CLOCK_MONOTONIC
 f28:   bl    7a0 <clock_gettime@plt>  ; 获取结束时间

;; ========================================
;; 第九部分：计算耗时并打印结果
;; ========================================
 f2c:   ldp   x0, x1, [sp, #8]         ; 加载开始时间
 f30:   ldr   x19, [sp, #24]           ; 加载结束时间（秒）
 f34:   sub   x19, x19, x0             ; x19 = 秒差
 f38:   ldr   x0, [sp, #32]            ; 加载结束时间（纳秒）
 f3c:   sub   x0, x0, x1               ; x0 = 纳秒差
 f40:   mov   x1, #0xca00              ; x1 = 1,000,000,000（纳秒转换常量）
 f44:   movk  x1, #0x3b9a, lsl #16
 f48:   madd  x19, x19, x1, x0         ; x19 = 总纳秒数
 f4c:   mov   x0, #0x848000000000
 f50:   movk  x0, #0x412e, lsl #48
 f54:   fmov  d31, x0                  ; d31 = 1,000,000.0（转毫秒）
 f58:   adrp  x0, 1000 <_fini+0x38>
 f5c:   add   x0, x0, #0xd8
 f60:   ucvtf d15, x19                 ; 转换为浮点数
 f64:   fdiv  d0, d15, d31             ; d0 = 毫秒数
 f68:   bl    7b0 <printf@plt>         ; 打印执行时间

 f6c:   mov   x0, #0x400000000000
 f70:   movk  x0, #0x409f, lsl #48
 f74:   fmov  d31, x0                  ; d31 = 2000.0（计算平均）
 f78:   adrp  x0, 1000 <_fini+0x38>
 f7c:   add   x0, x0, #0x358
 f80:   fdiv  d0, d15, d31             ; d0 = 平均每次
 f84:   bl    7b0 <printf@plt>         ; 打印平均时间

;; ========================================
;; 第十部分：函数返回 - 恢复栈空间和寄存器
;; ========================================
 f88:   adrp  x0, 1f000 <__FRAME_END__+0x1d674>
 f8c:   ldr   x0, [x0, #4024]
 f90:   add   x2, sp, #0xc8, lsl #12
 f94:   ldr   x3, [x2, #40]            ; 读取栈保护金丝雀值
 f98:   ldr   x1, [x0]
 f9c:   subs  x3, x3, x1               ; 检查是否被篡改
 fa0:   mov   x1, #0x0
 fa4:   b.ne  fc4 <...+0x184>          ; 如果被篡改，跳转到错误处理

 fa8:   add   sp, sp, #0x30            ; ⭐ sp += 48（恢复局部变量空间）
 fac:   mov   x0, x19                  ; x0 = 返回值（总纳秒数）
 fb0:   add   sp, sp, #0xc8, lsl #12   ; ⭐ sp += 800KB（恢复数组空间）
 fb4:   ldr   x19, [sp, #16]           ; 恢复 x19 寄存器
 fb8:   ldr   d15, [sp, #24]           ; 恢复 d15 寄存器
 fbc:   ldp   x29, x30, [sp], #32      ; 恢复帧指针和返回地址，sp += 32
 fc0:   ret                             ; 返回

 fc4:   bl    7f0 <__stack_chk_fail@plt> ; 栈保护检查失败
```

## 偏移地址读写详解

汇编代码中有多处使用了**偏移地址**访问内存，这是 ARM64 的典型寻址方式。

### 1. 数组基地址计算（基址偏移）

```asm
 ed0:   add   x0, sp, #0x28            ; ⭐ x0 = sp + 0x28（偏移寻址）
```

**说明**：
- `sp` 是基址（栈指针）
- `#0x28` (40字节) 是偏移量
- 结果：x0 = 数组基地址 = sp + 40

**为什么需要偏移 0x28？**
- 栈上先保存了寄存器和局部变量（约 48 字节）
- 数组从 sp + 0x28 开始，避开了这些数据

### 2. 页首/页尾访问（立即数偏移）

#### 页首访问（偏移 0）

```asm
 ee0:   strb  w1, [x0]                 ; 写页首: *(x0 + 0) = w1
 eec:   ldrb  w3, [x0]                 ; 读页首: w3 = *(x0 + 0)
```

**说明**：
- `[x0]` 等价于 `[x0, #0]`，即基址 x0，偏移为 0

#### 页尾访问（正向偏移 4095）

```asm
 ee4:   strb  w1, [x0, #4095]          ; ⭐ 写页尾: *(x0 + 4095) = w1（偏移寻址）
```

**说明**：
- `[x0, #4095]` 是 ARM64 的**基址+立即数偏移**寻址方式
- `x0` 是基址（当前页起始地址）
- `#4095` 是立即数偏移（页内最后一个字节，因为 4096-1=4095）
- 结果：访问地址 = x0 + 4095

**为什么是 4095？**
- 每页大小 = 4096 字节（0x1000）
- 页内地址范围：[0, 4095]
- 访问页尾确保整个页被映射到物理内存

#### 页尾访问（负向偏移 -1）

```asm
 f00:   ldurb w3, [x0, #-1]            ; ⭐ 读页尾: w3 = *(x0 - 1)（负偏移寻址）
```

**说明**：
- `ldurb` 是**未对齐加载字节**指令（允许负偏移）
- `[x0, #-1]` 使用负偏移
- 此时 x0 已经移动到**下一页**（通过 `add x0, x0, #0x1, lsl #12` 完成）
- 所以 `x0 - 1` 实际访问的是**前一页的最后一个字节**

**为什么用负偏移？**
- 地址 `ef0` 已将 x0 移到下一页：`x0 += 4096`
- 要读前一页的页尾，用 `x0 - 1` 更高效，避免额外的地址计算

### 3. 移动到下一页（左移偏移）

```asm
 ef0:   add   x0, x0, #0x1, lsl #12    ; ⭐ x0 += (1 << 12) = x0 + 4096（移位偏移）
```

**说明**：
- `#0x1, lsl #12` 表示 1 左移 12 位 = 1 × 2^12 = 4096
- 这是 ARM64 的**移位立即数**特性，高效计算 4096 的倍数
- 结果：x0 移动到下一页的起始地址

**为什么用左移而不是直接 `add x0, x0, #4096`？**
- ARM64 的立即数有范围限制（12位，最大 4095）
- 4096 超过了这个范围，所以用 `1 << 12` 来表示
- 更通用的写法是 `#0x1, lsl #12`（也可以是 `#0x2, lsl #12` = 8192）

### 4. 数组边界计算（左移偏移）

```asm
 ebc:   add   x5, sp, #0xc8, lsl #12   ; ⭐ x5 = sp + (0xc8 << 12) = sp + 819200（偏移寻址）
 ec4:   add   x5, x5, #0x28            ; x5 += 40（调整边界）
```

**说明**：
- `#0xc8, lsl #12` = 0xc8 × 4096 = 200 × 4096 = 819,200 字节 = 800KB
- x5 = 数组末尾边界地址
- 用于内层循环的边界检查：`cmp x5, x0`

### 5. 栈保护金丝雀（偏移访问）

```asm
 e60:   add   x1, sp, #0xc8, lsl #12   ; x1 = sp + 800KB
 e68:   str   x2, [x1, #40]            ; ⭐ *(x1 + 40) = canary（偏移写入）
 ...
 f94:   ldr   x3, [x2, #40]            ; ⭐ w3 = *(x2 + 40)（偏移读取）
```

**说明**：
- `[x1, #40]` 在基址 x1 偏移 40 字节处存储栈保护值
- 函数返回前读取并检查是否被篡改

## 偏移寻址模式总结

| 汇编指令格式 | 寻址方式 | 计算公式 | 示例 |
|------------|---------|---------|------|
| `[base]` | 基址寻址 | base + 0 | `[x0]` |
| `[base, #offset]` | 基址+立即数偏移 | base + offset | `[x0, #4095]` |
| `[base, #-offset]` | 基址+负偏移 | base - offset | `[x0, #-1]` |
| `add rd, base, #imm, lsl #n` | 基址+移位偏移 | base + (imm << n) | `sp + (#0xc8 << 12)` |

## 内存访问示意图

```
栈布局（从高地址到低地址）：

高地址
  ↑
  │
  ├─ sp (初始)
  │
  ├─ [保存的寄存器] (32 bytes)
  │
  ├─ [局部变量] (48 bytes)
  │
  ├─ sp (分配后) ← 当前 sp 位置
  │
  ├─ sp + 0x28 ← ⭐ 数组基地址 (x0 初始值)
  │    │
  │    ├─ [x0] ← 第1页页首（偏移 0）
  │    ├─ [x0, #4095] ← 第1页页尾（偏移 4095）
  │    │
  │    ├─ [x0 + 4096] ← 第2页页首（x0 移动后）
  │    ├─ [x0, #4095] ← 第2页页尾（偏移 4095）
  │    │
  │    │... 共 200 页 ...
  │    │
  │    └─ 第200页
  │
  ├─ sp + 0xc8028 ← ⭐ 数组末尾 (x5)
  │
  └─ 栈底

低地址
  ↓
```

## 核心指令列表

### 1. 栈空间分配（只执行一次）

| 地址 | 指令 | 说明 |
|------|------|------|
| `e58` | `sub sp, sp, #0x30` | 分配 48 字节局部变量空间 |
| **`e5c`** | **`sub sp, sp, #0xc8, lsl #12`** | **⭐ 一次性分配 800KB = 0xc8000 字节** |

### 2. 外层循环入口（重复10次）

| 地址 | 指令 | 说明 |
|------|------|------|
| `ecc` | `mov w6, #0xa` | w6 = 10（外层循环计数器初始化）|
| **`ed0`** | **`add x0, sp, #0x28`** | **⭐ x0 = 数组基地址 = sp + 0x28** |
| `ed4` | `mov w1, #0x42` | w1 = 0x42（初始写入值）|

**关键点**：地址 `ed0` 是外层循环的入口，每次迭代都从这里开始，由于 `sp` 不变，所以 `x0` 每次都指向相同地址。

### 3. 内层循环体（遍历200页）

| 地址 | 指令 | 说明 |
|------|------|------|
| **`ee0`** | **`strb w1, [x0]`** | **写页首（触发缺页）** |
| **`ee4`** | **`strb w1, [x0, #4095]`** | **写页尾（确保整页被映射）** |
| `ee8` | `add w1, w1, #0x1` | 递增写入值 |
| **`eec`** | **`ldrb w3, [x0]`** | **读页首** |
| **`ef0`** | **`add x0, x0, #0x1, lsl #12`** | **⭐ 移动到下一页（+4096）** |
| `ef4-efc` | 累加到全局计数器 | 防止编译器优化 |
| **`f00`** | **`ldurb w3, [x0, #-1]`** | **读页尾** |
| `f04-f0c` | 累加到全局计数器 | 防止编译器优化 |
| **`f10`** | **`cmp x5, x0`** | **检查是否到达数组末尾** |
| **`f14`** | **`b.ne ee0`** | **⭐ 跳回内层循环开始** |

### 4. 外层循环控制

| 地址 | 指令 | 说明 |
|------|------|------|
| **`f18`** | **`subs w6, w6, #0x1`** | **⭐ 外层计数器递减** |
| **`f1c`** | **`b.ne ed0`** | **⭐⭐⭐ 跳回外层循环入口（绕过栈分配）** |

**最关键的指令**：`f1c` 跳转到 `ed0`，而不是函数开始，这意味着跳过了地址 `e5c` 的栈分配代码！

### 5. 栈空间恢复（只执行一次）

| 地址 | 指令 | 说明 |
|------|------|------|
| `fa8` | `add sp, sp, #0x30` | 恢复 48 字节局部变量空间 |
| **`fb0`** | **`add sp, sp, #0xc8, lsl #12`** | **⭐ 恢复 800KB 栈空间** |
| `fbc` | `ldp x29, x30, [sp], #32` | 恢复帧指针和返回地址 |
| `fc0` | `ret` | 返回 |

## 控制流图

```
函数入口 (e40)
    ↓
保存寄存器 (e40-e54)
    ↓
⭐ 分配 800KB 栈空间 (e58-e5c) ← 只执行一次！
    ↓
打印信息 (e70-eac)
    ↓
开始计时 (eb0-eb8)
    ↓
初始化外层循环 (ebc-ecc)
    ↓
┌─→ 外层循环入口 (ed0) ⭐ ← 每次迭代从这里开始
│     ↓
│   ┌─→ 内层循环 (ee0-f0c)
│   │     - 写页首/页尾
│   │     - 读页首/页尾
│   │     - 移动到下一页
│   │     ↓
│   └── 检查是否完成200页 (f10-f14)
│     ↓
└── 检查是否完成10次 (f18-f1c) ⭐ 跳回 ed0，不重新分配栈！
    ↓
结束计时 (f20-f28)
    ↓
计算并打印结果 (f2c-f84)
    ↓
⭐ 恢复 800KB 栈空间 (fa8-fb0) ← 只执行一次！
    ↓
恢复寄存器并返回 (fb4-fc0)
```

## 关键验证点

### ✅ 验证1：栈空间只分配一次
- **分配指令**：地址 `e5c` 的 `sub sp, sp, #0xc8, lsl #12`
- **执行次数**：1次（在外层循环之前）

### ✅ 验证2：外层循环不重新分配栈
- **外层循环入口**：地址 `ed0`
- **循环跳转**：地址 `f1c` 的 `b.ne ed0`
- **跳转目标**：直接跳到 `ed0`，跳过了 `e5c` 的栈分配代码

### ✅ 验证3：数组基地址固定不变
- **计算基地址**：地址 `ed0` 的 `add x0, sp, #0x28`
- **sp 的值**：在整个外层循环（10次迭代）中保持不变
- **结果**：每次迭代 `x0` 都是相同的地址，访问相同的 800KB 栈内存

### ✅ 验证4：内层循环触发缺页
- **写操作**：`ee0` (写页首) + `ee4` (写页尾)
- **缺页时机**：
  - 第1次外层循环：访问200个新页 → 触发约200次缺页
  - 第2-10次外层循环：访问相同的200页 → 页表已存在，缺页 ≈ 0
- **总缺页数**：约200次

## 性能分析

### 内存访问模式

```
第1次外层循环:
  sp + 0x28 + 0x0000 ← 第1页（缺页）
  sp + 0x28 + 0x1000 ← 第2页（缺页）
  ...
  sp + 0x28 + 0xc7000 ← 第200页（缺页）

第2次外层循环:
  sp + 0x28 + 0x0000 ← 第1页（命中，sp 未变！）
  sp + 0x28 + 0x1000 ← 第2页（命中）
  ...
  sp + 0x28 + 0xc7000 ← 第200页（命中）

...重复10次...
```

### 对比场景2

- **场景2**：顺序访问400个不同的页 → 400次缺页
- **场景3**：重复访问200个相同的页10次 → 200次缺页

## 总结

汇编代码完美验证了优化假设：

1. ✅ 栈空间在函数序言中一次性分配（`e5c`）
2. ✅ 外层循环跳转绕过栈分配代码（`f1c → ed0`）
3. ✅ sp 在整个外层循环中保持不变
4. ✅ 每次迭代访问相同的 800KB 栈内存
5. ✅ 实现了栈空间复用机制

这是编译器**循环不变量外提**（Loop-Invariant Code Motion）优化的典型案例。
